# LWA audit snapshot — 16 Sep 2026

Full report: `/workspace/LWA-AUDIT-2026-09-16.md` (also mirrored under repo root if copied).

## Top findings (evidence)

1. **Soft-404 homepage OG** on live unknown blog, bare blog slug, and `/arthritis-support/london` (`curl` 200 + homepage title/canonical). Sheffield city×condition already had a redirect stub.
2. **Gift Aid overclaim** on homepage donate bands while `CHARITY.giftAidRegistered === false`.
3. **Lovable bot** re-added `src/integrations/supabase/**` on `3977822` — removed again (no restore).
4. **CI red**: prerender meta zero-tolerance on intentional noindex app screens; ESLint any/control-regex; blog-smoke JSON import; Playwright stale selectors; Lighthouse double-bind on :4173.
5. **Live CSP** still only `frame-ancestors 'self'` — Louis/host dashboard (repo `_headers` already full).

## Fixed in this ship

- Unique **noindex city hub** HTML postbuild + city×condition / bare-blog / `/uk/:city/:service` redirect stubs
- Gift Aid honesty on InspiredHeroBand / DonationImpact / FinalDonateBand / WaysToHelp / About FAQ
- Delete Supabase restore; ESLint + blog-smoke + Playwright + Lighthouse port + prerender noindex allowlist
- Disclaimer strips on HCP, Resource Centre, OA, Exercise hub; 404 skip-link target

## Still needs Louis

Lovable **publish** (Deploy gated on green CI), host **CSP** dashboard, HMRC Gift Aid flip + Fundraising Regulator, clinical spot-check.
