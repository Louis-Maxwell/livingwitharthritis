## Goal

Three things: (1) broaden structured-data coverage so more pages emit Breadcrumb + FAQ rich-result schemas, (2) submit the live sitemap to Google Search Console and verify `/chat` is indexable while private routes stay blocked, (3) push the refreshed `sitemap.xml` + `robots.txt` live.

## Current state (verified)

- `index.html` already injects sitewide **MedicalOrganization** JSON-LD (line 150) — no change needed there.
- `src/components/seo/PageSchema.tsx` exists and supports `BreadcrumbList` + `FAQPage` + `MedicalWebPage` via `useEffect` (memory-compliant — no Helmet JSON-LD).
- Only 5 routes currently use `PageSchema` (the 4 pillar guides + TaiChiForBalance). ~70 other pages render no breadcrumb/FAQ schema.
- `public/sitemap.xml` already contains **456 `<url>` entries**, generated by `scripts/generate-sitemap.ts` (predev/prebuild). Nothing to regenerate.
- `public/robots.txt` correctly allows `/chat` and disallows `/admin/`, `/auth`, `/donation-result`, `/unsubscribe`, `/newsletter/confirm`, `/debug/`, `/site-index` for all major bots + wildcard.
- Google Search Console connector is **linked** to this project (connection verified).

## 1. Broaden Breadcrumb + FAQ schema coverage

Add `<PageSchema />` to the high-value routes that currently lack it. Pass `breadcrumbs` derived from the route hierarchy; pass `faqs` where the page already renders an FAQ accordion (avoid duplicating answers — schema must mirror visible content per Google's policy).

Priority routes (highest traffic per analytics + ranked landing pages):

- `Index.tsx` (`/`) — Organization is in `index.html`; add `BreadcrumbList` (Home only ⇒ skip) and `FAQPage` only if landing has visible FAQ section. (Check `FAQSection` import — if present on Index, attach FAQ schema; otherwise skip FAQ on root.)
- `AboutUs.tsx` — Breadcrumbs `Home › About`.
- `DietHub.tsx`, `ExerciseHub.tsx`, `SelfHelpTool.tsx`, `ArthritisFlareUps.tsx`, `Chat.tsx`, `FAQ.tsx` — Breadcrumbs + FAQ (FAQ.tsx renders `FAQSection`, so attach full FAQ schema built from the same `faqItems` source).
- Condition pages under `src/pages/conditions/*` and `CityConditionPage.tsx`, `CityArthritisPage.tsx` — Breadcrumbs `Home › Conditions › <name>` + MedicalWebPage with `medical.condition`.
- Blog: `BlogPost.tsx`, `BlogHub.tsx`, `BlogIndex.tsx`, `BlogCategory.tsx` — Breadcrumbs reflecting category/post.
- Pillar pages already covered — leave as is.

Implementation pattern (single line of import + one JSX node per page):

```tsx
<PageSchema
  url="/diet"
  name="Anti-Inflammatory Diet for Arthritis (UK)"
  description="..."
  breadcrumbs={[{ name: "Home", item: "/" }, { name: "Diet Hub" }]}
  faqs={dietFaqs}
  medical={{ condition: "Arthritis" }}
/>
```

For `FAQ.tsx`, source the `faqs` array from the same data file that `FAQSection` reads so the visible Q&A and the schema stay synchronised.

No new components; no design changes; no business-logic changes.

## 2. Submit sitemap to Google Search Console + verify indexability

Run the following via the GSC connector gateway (credentials already linked):

1. `PUT /webmasters/v3/sitemaps/https%3A%2F%2Flivingwitharthritis.org.uk%2F/sitemap.xml` — registers the sitemap.
2. `GET /webmasters/v3/sitemaps/...` — confirm `lastSubmitted`, `contents`, and zero `errors`/`warnings` on the parsed file.
3. `POST /v1/urlInspection/index:inspect` for:
   - `/chat` — expect `verdict: PASS`, `coverageState` indicating indexable.
   - `/admin/`, `/auth`, `/donation-result` — expect `robotsTxtState: DISALLOWED`.
   Report results back in chat (no UI change).

## 3. Publish refreshed sitemap.xml + robots.txt

Both files are already up to date in `public/` and will ship on the next deploy. I cannot click Publish for you; after the schema edits are merged I'll surface the publish action so the 456-entry sitemap + current robots.txt go live in one deploy.

## Out of scope

- Regenerating sitemap (no route/data changes warrant it; current 456 entries are current).
- Editing `robots.txt` (already correct — `/chat` allowed, private routes blocked).
- Adding new pages or FAQ content.

## Verification

- Build passes (typecheck).
- Spot-check 3 pages in browser DevTools: `document.querySelectorAll('script[type="application/ld+json"]').length >= 2` (sitewide Org + per-page BreadcrumbList/FAQPage).
- Paste 1 page's JSON-LD into Google's Rich Results Test (manual — I'll provide the URL list).
- GSC sitemap status returns success; URL Inspection confirms `/chat` indexable and `/admin/` blocked.
