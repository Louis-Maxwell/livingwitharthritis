Remove the eyebrow badge ("You are not alone · A gentle plan for living well") from `src/components/landing/OAHero.tsx`.

Delete the entire `<div className="hero-item mb-8">…</div>` wrapper (lines ~44–54) containing the pulsing red dot and the uppercase tagline. The headline below becomes the first element in the hero content stack.

No other changes — headline, paragraph, CTAs, trust row, and image stay exactly as they are.