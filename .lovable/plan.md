## Goal
Shared links (Facebook, LinkedIn, WhatsApp, X, Slack) should show the new stick-figure + "LIVING WITH ARTHRITIS" branding.

## Current state (verified)
- `index.html` sets `og:image` / `twitter:image` to `https://livingwitharthritis.org.uk/og/home.png`.
- `SeoHead.tsx` defaults every page's `og:image` to a stock photo (`/images/hero-walking-group-1600.webp`), not a branded card.
- `public/og/*.png` cards are generated by `scripts/generate-og-images.ts` (satori) and contain **text only** — no logo mark, and they use the old crimson `#c1121f` rather than the current brand red.

## Changes

1. **Add the logo to the OG card template** (`scripts/generate-og-images.ts`)
   - Draw the stick-figure mark as inline SVG shapes in the satori template (same geometry as `SiteLogo.tsx`), placed in the header row beside the category label, plus the wordmark in the footer row.
   - Update the accent colour to the current brand red (HSL 354 85% 42%) so cards match the site.

2. **Regenerate all cards**
   - Run the generator with `--force` so every existing `public/og/*.png` is rebuilt with branding (≈ all condition, guide, hub and home cards).

3. **Make the branded card the sitewide default** (`src/components/SeoHead.tsx`)
   - Change `DEFAULT_IMAGE` from the stock hero photo to `https://livingwitharthritis.org.uk/og/home.png`, so any page not passing an explicit image shares the branded card.
   - Update `og:image:alt` default wording to reference the brand.

4. **Keep static head in sync** (`index.html`)
   - Confirm `og:image` / `twitter:image` still point at the regenerated `/og/home.png`; refresh the `og:image:alt` text to match.

5. **Verify**
   - Production build passes.
   - Spot-check a regenerated PNG visually (1200×630, logo legible).

## Note
Facebook/LinkedIn/X cache previews. After publishing, the old image may persist until each platform re-scrapes; it can be forced via their link-preview debuggers.
