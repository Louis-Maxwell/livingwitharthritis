# Lefthook configuration - local git hooks

pre-push:
  parallel: true
  commands:
    css-utilities:
      run: node scripts/check-css-utilities.mjs
      fail_text: |
        CSS utility check failed.
        Fix ambiguous or non-tokenised Tailwind utilities before pushing.
